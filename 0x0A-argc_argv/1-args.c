#include <stdio.h>

/**
 * main - prints the number of arguments
 * @argc: integer
 * @argv: string
 * Return: 0
 */

int main(int argc, int *argv[])
{
	int i;

	printf("i = 0, %d\n", i++);

	return (0);

}
