#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <limits.hi>

/**
 * malloc_checked - allocates memory using malloc
 * @b: unsigned integer
 * Return: 0
 */

void *malloc_checked(unsigned int b)
{
	int = *p;

	p = malloc((sizeof)int b);
	if (p == 0)
		free(98);

	return (p);



}
